spring:
  application:
    name: microid
  datasource:
    url: jdbc:mysql://localhost:3306/microid
    driverClassName: com.mysql.cj.jdbc.Driver
    username: microidapp
    password: microid
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  data:
    jpa:
      repositories:
        enabled: true
  security:
    jwt:
      secretKey: E8WCBRh7uYNLOwIR7fgsl2DV8Ggnplxm
      expiration: 900000

management:
  endpoint:
    health:
      show-details: always

